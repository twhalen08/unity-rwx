using UnityEngine;

namespace RWXLoader
{
    /// <summary>
    /// Stores the RWX tag assigned to a mesh so VP actions can target specific surfaces.
    /// </summary>
    public class RWXTag : MonoBehaviour
    {
        public int Tag;
    }
}
